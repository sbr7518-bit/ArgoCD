apiVersion: apps/v1
kind: Deployment
metadata:
  name: demo
  namespace: demo
spec:
  replicas: 1
  selector:
    matchLabels:
      app: demo-container
  template:
    metadata:
      labels:
        app: demo-container
    spec:
      containers:
        - name: demo-container
          image: docker.io/koreanirsa/demo:PLACEHOLDER
          ports:
            - containerPort: 8080
          envFrom:
            - configMapRef:
              name: demo-config









